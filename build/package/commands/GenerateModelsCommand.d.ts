/**
 * Generates models for the given graphql schemas.
 */
export declare class GenerateModelsCommand {
    command: string;
    describe: string;
    builder(yargs: any): any;
    handler(argv: any): Promise<void>;
}
