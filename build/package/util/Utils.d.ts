/**
 * Utility functions used in the codebase.
 *
 * @internal
 */
export declare class Utils {
    /**
     * Loads all exported classes from the given directory.
     */
    static importClassesFromDirectories(directories: string[], formats?: string[]): Function[];
}
